## 1. README content refresh

- [x] 1.1 Update the README overview, workflow, and feature summary so they describe the current explorer layout and separate `CLI Tools` view.
- [x] 1.2 Replace outdated command wording with documentation for contextual copy actions and the `default` vs `Claude Code` syntax modes.

## 2. Screenshot integration

- [x] 2.1 Embed `media/screenshot.png` in `README.md` with suitable alt text and surrounding copy.
- [x] 2.2 Ensure the screenshot description highlights the explorer, change action menu, and `CLI Tools` view without depending on theme-specific details.

## 3. Verification

- [x] 3.1 Cross-check the README copy against `openspec/specs/explorer-command-surfaces/spec.md` and `openspec/specs/artifact-clipboard-export/spec.md`.
- [x] 3.2 Preview the updated markdown and confirm no obsolete `/opsx:*` phrasing remains.
