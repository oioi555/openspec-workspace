## Why

The README no longer matches the current extension UI after the explorer command surface refinement. It still describes the older command-copy flow, does not explain the separate `CLI Tools` view, and does not show the current explorer layout.

## What Changes

- Update the README overview, feature summary, workflow, and quickstart sections so they describe the current explorer structure and command-copy behavior.
- Document the two supported command syntax styles, with `default` using `/opsx-<command>` and `Claude Code` using `/opsx:<command>`.
- Add a screenshot section that embeds `media/screenshot.png` and highlights the current explorer, context menu, and `CLI Tools` view.

## Capabilities

### New Capabilities
- `readme-feature-overview`: The repository README documents the current explorer layout, command-copy workflow, and visual UI preview for the extension.

### Modified Capabilities

## Impact

- `README.md`
- `media/screenshot.png` (existing asset referenced from the README)
- Documentation copy for the explorer workflow, command examples, and feature overview
